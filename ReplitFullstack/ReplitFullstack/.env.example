# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration (optional - only needed if using database)
# DATABASE_URL=postgresql://user:password@localhost:5432/portfolio

# Replit Configuration (optional)
# REPL_ID=
